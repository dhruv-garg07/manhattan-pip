name: Build and Upload Protected Wheels

on:
  push:
    tags:
      - 'v*'

jobs:
  build_wheels:
    name: Build wheels on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    defaults:
      run:
        working-directory: ./manhattan-mcp

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install build dependencies
        # 'build' is the most reliable frontend for PEP 517 binary packages
        run: pip install cython setuptools wheel cibuildwheel build

      - name: Build wheels
        shell: bash
        run: python -m cibuildwheel --output-dir ../wheelhouse
        env:
          # Using 'build' ensures your setup.py extensions are correctly detected
          CIBW_BUILD_FRONTEND: "build"
          CIBW_BEFORE_BUILD: "pip install cython setuptools wheel"
          CIBW_BUILD: "cp310-* cp311-* cp312-*"
          CIBW_SKIP: "pp* *-musllinux*"
          # This helps verbose logging to debug the compilation phase
          CIBW_BUILD_VERBOSITY: "1"

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: cibw-wheels-${{ matrix.os }}-${{ strategy.job-index }}
          path: wheelhouse/*.whl

  upload_pypi:
    needs: [build_wheels]
    runs-on: ubuntu-latest
    # This ensures we only attempt upload if at least one wheel was built
    if: success()
    steps:
      - uses: actions/download-artifact@v4
        with:
          pattern: cibw-wheels-*
          path: dist
          merge-multiple: true

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          user: __token__
          password: ${{ secrets.PYPI_API_TOKEN }}